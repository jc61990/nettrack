# NetTrack Python dependencies — Fedora 43 / Python 3.14
#
# IMPORTANT: On Fedora 43, install compiled packages via dnf first
# (handled automatically by setup_fedora43.sh), then pip for the rest.
#
# Packages with C/Rust extensions come from dnf to avoid build failures:
#   dnf install python3-sqlalchemy python3-psycopg2 python3-pyyaml
#               python3-requests python3-httpx python3-cryptography
#               python3-bcrypt python3-pydantic
#
# Pure Python packages installed via pip:
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
python-jose==3.3.0
python-multipart==0.0.12
slowapi==0.1.9
pysnmp==6.1.4
apscheduler==3.10.4
alembic==1.13.1
